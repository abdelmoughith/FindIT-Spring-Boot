package pack.smartwaste.config;
/*
import com.google.auth.oauth2.GoogleCredentials;
import com.google.firebase.FirebaseApp;
import com.google.firebase.FirebaseOptions;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.core.io.ClassPathResource;

import java.io.IOException;
import java.io.InputStream;
*/
//@Configuration
public class FirebaseConfig {
/*
    @Bean
    FirebaseApp firebaseApp() throws IOException {
        // Load the service account key JSON file from the classpath
        ClassPathResource resource = new ClassPathResource("serviceAccountKey.json");
        InputStream serviceAccount = resource.getInputStream();

        // Set Firebase options using the service account credentials
        FirebaseOptions options = FirebaseOptions.builder()
                .setProjectId("indus-532b7")
                .setCredentials(GoogleCredentials.fromStream(serviceAccount))
                .setStorageBucket("gs://indus-532b7.appspot.com")
                .setDatabaseUrl("https://indus-532b7-default-rtdb.firebaseio.com")
                .build();

        // Initialize Firebase
        return FirebaseApp.initializeApp(options);
    }

 */
}


